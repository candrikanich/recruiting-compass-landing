{
  "$comment": "Proxy /resources/* to the separate recruiting-compass-resources Vercel deployment. Replace RESOURCES_DEPLOYMENT_HOST with the resources project's production domain (e.g. recruiting-compass-resources.vercel.app) after that project is created. Keep the /resources prefix — the resources app sets app.baseURL '/resources', so its assets live at /resources/_nuxt/*.",
  "rewrites": [
    {
      "source": "/resources",
      "destination": "https://RESOURCES_DEPLOYMENT_HOST/resources"
    },
    {
      "source": "/resources/:path*",
      "destination": "https://RESOURCES_DEPLOYMENT_HOST/resources/:path*"
    }
  ]
}
